dist: trusty
os:
    - linux

git:
    quiet: true
branches:
    only: master
    
language: c
compiler:
    - gcc
env:
    - DESTDIR="_install"

script:
    - ./configure
    - make
    - make DESTDIR=$DESTDIR test
